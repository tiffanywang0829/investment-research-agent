entrypoint = "investment_agent/agent.py"
run = "pip install -q -r requirements.txt && adk web investment_agent --host 0.0.0.0 --port 8080"
modules = ["python-3.11"]

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "pip install -q -r requirements.txt && adk web investment_agent --host 0.0.0.0 --port 8080"]
deploymentTarget = "cloudrun"
